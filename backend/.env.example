# ─────────────────────────────────────────────────────────────────
#  AdFlow Pro — Backend Environment Variables
#  Copy this file to .env and fill in your values.
#  NEVER commit the actual .env file to GitHub.
# ─────────────────────────────────────────────────────────────────

# Server
PORT=5000

# MongoDB — get a free cluster at https://cloud.mongodb.com
MONGODB_URI=mongodb+srv://meharbanali:meharbanali123.@backend-work.plxh37v.mongodb.net/adflow_pro?retryWrites=true&w=majority

# JWT — use a long random string (min 32 chars)
# Generate one: node -e "console.log(require('crypto').randomBytes(48).toString('hex'))"
JWT_SECRET=2771c6f41636e4e89af82115ec4c122776f65c3e75c7856070e365e77abefe89

# Frontend origin (for CORS)
CLIENT_URL=http://localhost:5173

# Cron jobs toggle (set to false to disable scheduled tasks)
CRON_ENABLED=true
